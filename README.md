# STR Limit

## How to use?

```sh
yarn add str-limit
# or
npm install str-limit
```

```js
const { str_limit } = require('str-limit');

str_limit('A string that will be limited', 12);
// A string...
```
